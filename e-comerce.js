valuesKey = (name, code, price) =>{
    img = (`<img href="https://www.pngwing.com/es/free-png-dkopa">`)
    name = (`Name: ${name}`);
    code = (`Code: ${code}`);
    price = (`Price: ${price} U$s`);
    return [name, code, price];
}